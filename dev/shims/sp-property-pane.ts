/** Stub for @microsoft/sp-property-pane — only used if webpack resolves it transitively. */

export interface IPropertyPaneConfiguration {
  pages: unknown[];
}

export interface IPropertyPaneTextFieldProps {
  label: string;
  description?: string;
}

export function PropertyPaneTextField(
  _targetProperty: string,
  _properties: IPropertyPaneTextFieldProps
): unknown {
  return {};
}
