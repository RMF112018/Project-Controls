{
  "name": "@hbc/auth",
  "version": "1.0.0",
  "private": true,
  "description": "Auth & permissions — Zustand stores, RBAC guards, feature flags, MSAL/SPFx adapters",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/ --ext .ts,.tsx",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "dependencies": {
    "@hbc/models": "workspace:*",
    "zustand": "^5.0.0"
  },
  "peerDependencies": {
    "react": "^18.2.0"
  }
}
