// @hbc/query-hooks — TanStack Query hooks per domain

// Query key factory (for cache invalidation and prefetching)
export { queryKeys } from './keys';

// Default query/mutation options
export { defaultQueryOptions } from './defaults';

// Domain hooks
export * from './leads';
export * from './schedule';
